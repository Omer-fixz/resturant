# دليل مكونات التطبيق

## 📱 الصفحات الرئيسية

### 1. صفحة تسجيل الدخول (`LoginPage.jsx`)
- **الموقع**: `client/src/App.jsx` (مدمجة)
- **الميزات**:
  - تسجيل دخول بالبريد وكلمة المرور
  - خاصية "تذكرني"
  - إعادة تعيين كلمة المرور
  - رسائل خطأ واضحة باللغة العربية

### 2. صفحة التسجيل (`RegisterPage.jsx`)
- **الموقع**: `client/src/App.jsx` (مدمجة)
- **الميزات**:
  - إنشاء حساب جديد
  - التحقق من صحة البيانات
  - حفظ بيانات المطعم في Firestore

### 3. لوحة التحكم (`Dashboard`)
- **الموقع**: `client/src/App.jsx` (مدمجة)
- **الميزات**:
  - عرض معلومات المطعم
  - أزرار سريعة للوصول للميزات
  - حالة الاتصال
  - إحصائيات سريعة

## 🍽️ إدارة المنيو

### 4. إدارة المنيو (`MenuManagement.jsx`)
- **الموقع**: `client/src/pages/MenuManagement.jsx`
- **الميزات**:
  - عرض جميع الوجبات
  - إضافة وجبة جديدة
  - تعديل الوجبات الموجودة
  - حذف الوجبات
  - تفعيل/تعطيل الوجبات
  - رفع الصور إلى Cloudinary

### 5. إضافة وجبة (`AddMeal.jsx`)
- **الموقع**: `client/src/pages/AddMeal.jsx`
- **الميزات**:
  - نموذج إضافة وجبة
  - رفع صورة الوجبة
  - حفظ في Firestore

## 📋 إدارة الطلبات

### 6. إدارة الطلبات (`OrdersManagement.jsx`)
- **الموقع**: `client/src/pages/OrdersManagement.jsx`
- **الميزات**:
  - عرض جميع الطلبات
  - تصفية الطلبات حسب الحالة
  - تحديث حالة الطلبات
  - إشعارات فورية للطلبات الجديدة
  - تتبع الطلبات في الوقت الفعلي

## 🔧 المكونات المساعدة

### 7. حماية الصفحات (`ProtectedRoute.jsx`)
- **الموقع**: `client/src/components/ProtectedRoute.jsx`
- **الوظيفة**: حماية الصفحات من الوصول غير المصرح

### 8. مؤشر قوة كلمة المرور (`PasswordStrength.jsx`)
- **الموقع**: `client/src/components/PasswordStrength.jsx`
- **الميزات**:
  - مؤشر بصري لقوة كلمة المرور
  - معايير متعددة للأمان
  - ألوان تدل على مستوى الأمان

## 🌐 الخدمات

### 9. خدمة المصادقة (`authService.js`)
- **الموقع**: `client/src/services/authService.js`
- **الوظائف**:
  - تسجيل الدخول والخروج
  - إنشاء حسابات جديدة
  - إعادة تعيين كلمة المرور
  - مراقبة حالة المصادقة

### 10. خدمة Firestore (`firestoreService.js`)
- **الموقع**: `client/src/services/firestoreService.js`
- **الوظائف**:
  - عمليات قاعدة البيانات
  - حفظ واسترجاع البيانات
  - تحديث وحذف السجلات

### 11. خدمة Cloudinary (`cloudinaryService.js`)
- **الموقع**: `client/src/services/cloudinaryService.js`
- **الوظائف**:
  - رفع الصور
  - تحسين الصور
  - إدارة الملفات

### 12. خدمة Socket.io (`socketService.js`)
- **الموقع**: `client/src/services/socketService.js`
- **الوظائف**:
  - اتصال فوري مع الخادم
  - إشعارات الطلبات الجديدة
  - تحديثات فورية

## 🎯 كيفية الوصول للمكونات

### من لوحة التحكم:
1. **إدارة المنيو**: اضغط على "إدارة المنيو" أو "إضافة وجبة"
2. **إدارة الطلبات**: اضغط على "عرض الطلبات" أو "طلبات جديدة"
3. **عرض المكونات**: اضغط على "عرض المكونات" لرؤية جميع الميزات

### الوصول المباشر:
```javascript
// في App.jsx يمكنك تغيير الصفحة الحالية
setCurrentPage('menu')     // إدارة المنيو
setCurrentPage('orders')   // إدارة الطلبات
setCurrentPage('demo')     // عرض المكونات
```

## 🔄 تدفق البيانات

### 1. المصادقة:
```
تسجيل الدخول → Firebase Auth → تحديث حالة المستخدم → تحميل بيانات المطعم
```

### 2. إدارة المنيو:
```
إضافة وجبة → رفع الصورة إلى Cloudinary → حفظ البيانات في Firestore → تحديث الواجهة
```

### 3. إدارة الطلبات:
```
طلب جديد → Socket.io إشعار → عرض في الواجهة → تحديث الحالة → حفظ في Firestore
```

## 🚀 الميزات المتقدمة

### 1. تحديث الأسعار بالجملة
- تحديث جميع أسعار الوجبات بنسبة مئوية
- مفيد لمواجهة التضخم

### 2. الإشعارات الفورية
- صوت تنبيه للطلبات الجديدة
- إشعارات بصرية
- تحديثات فورية للحالة

### 3. إدارة الصور
- رفع تلقائي إلى Cloudinary
- تحسين الصور للويب
- معاينة فورية

### 4. حالة الاتصال
- مراقبة الاتصال بالإنترنت
- عرض حالة الخادم
- إعادة الاتصال التلقائي

## 🔧 المشكلة الحالية

**المشكلة**: لا تظهر بيانات المطعم بسبب قواعد Firestore

**الحل**: تحديث قواعد Firestore في Firebase Console:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

**بعد الحل**: ستعمل جميع المكونات بشكل كامل وستظهر بيانات المطعم المحفوظة.

## 📱 الاستخدام الحالي

حتى مع وجود المشكلة، يمكنك:
- ✅ تسجيل الدخول والخروج
- ✅ الوصول لإدارة المنيو (بدون حفظ دائم)
- ✅ الوصول لإدارة الطلبات
- ✅ رؤية جميع الواجهات والتصاميم
- ✅ اختبار جميع الميزات

**ملاحظة**: البيانات لن تُحفظ بشكل دائم حتى يتم حل مشكلة قواعد Firestore.